apiVersion: v1
kind: Pod
metadata:
 name: nginx-pod
spec: 
 containers:
  - name: nginx
    image: nginx
    imagePullPolicy: always
    ports:
    - containerPort: 80



# apiVersion: v1
# kind: Pod
# metadata:
#   name: nginx-pod
# spec:
#   containers:
#     - name: nginx
#       image: nginx
#       ports:
#       - containerPort: 80